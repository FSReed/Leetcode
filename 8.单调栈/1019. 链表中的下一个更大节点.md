# [Description](https://leetcode.cn/problems/next-greater-node-in-linked-list/description/)

## Code

区别在于无法提前确定链表大小，所以要注意实时扩充answer数组  

> 最能感受到脑梗的一次，for循环里面的更新与循环结束条件写反了，调了20分钟，我是sb

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    vector<int> nextLargerNodes(ListNode* head) {
        int curr = 0;
        std::vector<int> answer;
        std::stack<std::pair<int, int>> stk;
        // 这个for循环卡了20分钟，之前是这么写的，一直没看出来有错：
        // for (; head = head->next; head) {...}
        // 纯sb，自己都气笑了
        for (; head; head = head->next) {
            while (!stk.empty() && head->val > stk.top().second) {
                auto [idx, val] = stk.top();
                answer[idx] = head->val;
                stk.pop();
            }
            stk.push({curr++, head->val});
            answer.push_back(0);
        }
        return answer;
    }
};
```
